{
    "http_port": <%= @port %>,
    "server_address": "<%= @server_name %>",

    "upstream_host": "registry.npmjs.org",
    "upstream_port": 80,

    "cache_dir": "<%= @cache_dir %>",
    "cache_mem": <%= @cache_mem %>

}
